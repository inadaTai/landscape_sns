<% provide(:title, '通知一覧') %>
<div>
  <h1 class="sub-title">通知一覧ページ</h1>
  <% if @notifications.exists? %>
    <div class="users-index">
      <%= render @notifications %>
      <%= will_paginate @notifications %>
    </div>
  <% else %>
    <p>通知はありません</p>
  <% end %>
</div>
